@model IEnumerable<WebHotelli.Models.Habitacion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.planta)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.numeracion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estado)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Categoria.nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.planta)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.numeracion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estado)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Categoria.nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_hab }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_hab }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_hab })
        </td>
    </tr>
}

</table>
